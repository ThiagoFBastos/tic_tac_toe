#ifndef GUI_HPP
#define GUI_HPP

#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>
#include "info.hpp"

class GUI {
	protected:
	virtual void dispose() = 0;
	public:
	GUI();
	virtual void build() = 0;
	virtual GAME_STATUS run() = 0;
};

#endif
